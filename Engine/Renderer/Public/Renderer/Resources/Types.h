#pragma once

#include <cstdint>

namespace Renderer
{
    using SummitResourceId = struct { uint32_t id; };
    using SummitBufferId = struct { uint32_t id; };
    using SummitImageId = struct { uint32_t id; };
    using SummitPipelineId = struct { uint32_t id; };
}
